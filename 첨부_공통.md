# PART 1 

## 게시물 ① — 크롤러 회복탄력성 (사이트 개편 대응)

```
"잘 돌던 수집기가 어느 날 갑자기 빈 데이터를 뱉기 시작했습니다."

정부 공공 데이터 분석 시스템을 운영하면서 가장 자주 만나는 장애 중 하나는
'외부 웹 매체의 개편'입니다. 수집 대상 사이트가 레거시 HTML에서
모던 SPA(React/Svelte 계열)로 바뀌면, 기존 DOM 셀렉터 기반 크롤링이
한순간에 무너집니다.

[문제]
- 목록은 긁히는데 본문·작성일·조회수가 비거나 깨짐
- 특정 수치(댓글수 등) 파싱 실패 시 빈 문자열이 그대로 들어가
  목록 화면 자체가 렌더링되지 않는 2차 장애로 번짐

[접근]
1) DOM 셀렉터 의존을 줄이기 위해 구조화 데이터(JSON-LD)를 1순위로 파싱하고,
   실패 시 HTML 추출로 떨어지는 '이중화(fallback)' 구조로 재설계
2) 게시판 유형마다 다른 마크업은 '특정 영역 존재 여부'로 분기 처리
3) 본문 품질은 메타태그(og:description) 우선 활용으로 노이즈 최소화
4) 모든 수치 필드에 '정규식으로 숫자만 추출 + 0 기본값' 예외처리 표준화
5) 구형 스크립트 엔진 호환을 위해 최신 문법 대신 보수적 루프 사용

[결과]
- 개편된 다수 매체의 수집 정상화, 빈값으로 인한 목록 장애 재발 0건
- 이후 신규 매체는 'JSON-LD 우선 → HTML 백업' 패턴을 기본 템플릿으로 채택

배운 것: 크롤러의 안정성은 '얼마나 정확한 셀렉터를 쓰느냐'보다
'구조가 바뀌어도 버티는 설계'에서 나온다. 외부 의존이 큰 시스템일수록
실패를 전제로 한 fallback 설계가 핵심이라는 걸 다시 배웠습니다.

#백엔드개발 #크롤링 #데이터엔지니어링 #공공SI #트러블슈팅 #JSONLD #유지보수
```

---

## 게시물 ② — 리포팅 툴 한글 다운로드 줄바꿈 커스터마이징

```
"보고서를 한글(HWP)로 내려받으면 줄바꿈이 제멋대로예요."

공공 시스템의 리포팅(상용 리포팅 툴) 산출물에서 들어온 요청입니다.
화면에선 멀쩡한데, 한글 파일로 다운로드하면 글자가 원하는 단위로
끊기지 않는 문제였습니다.

[요구사항]
- 한글 파일 다운로드 시 '글자 단위' 또는 '어절 단위' 줄바꿈을
  의도대로 제어할 수 있는가?

[접근]
- 리포팅 툴의 출력 속성과 스크립트를 단계적으로 변경하며
  글자 단위/어절 단위 줄바꿈 동작을 케이스별로 검증
- 속성 변경만으로 안 되는 부분은 스크립트 레벨에서 보정,
  1차·2차 수정안을 만들어 산출물로 비교 리포트 작성

[배운 것]
상용 툴 커스터마이징은 '되는지/안 되는지'를 빠르게 가려내는
실험 설계가 절반입니다. 무작정 만지기보다 변경 → 출력 → 비교를
기록으로 남기니, 발주처와의 합의도 훨씬 빨라졌습니다.

작은 줄바꿈 하나도 '문서로 일하는 공공 현업'에겐 품질입니다.

#공공SI #리포팅툴 #문제해결 #유지보수 #SM
```

---

## 게시물 ③ — 레거시 분석: 기능분해도(FP) 산출

```
"이 시스템, 도대체 기능이 몇 개일까?"

차세대 전환을 앞두고 노후 시스템(ASP/JSP 기반)의 전체 그림을
파악해야 했습니다. 그래서 직접 기능점수(FP) 간이법 기준으로
전 화면을 '사용자 관점의 단위 프로세스'로 분해했습니다.

[작업]
- 대/중/소 메뉴 단위로 전 기능을 분해하고
  각 기능의 트랜잭션 유형(C/R/U/D)을 분류
- 물리삭제 vs 논리삭제를 구분 표기 (행정 데이터 운영 원칙 반영)
- 외부 API 연계 여부를 기능별로 판별
- 각 기능이 참조하는 테이블을 추적 (JOIN 포함 전수 기재)

[결과물]
- 두 개 시스템에 걸쳐 수백 개 단위기능의 기능분해도 산출
- 차세대 이관·견적의 기준 문서로 활용

[배운 것]
주니어 시절 '코드만 잘 짜면 된다'고 생각했는데,
시스템 전체를 한 장으로 구조화하는 분석 역량이
설계·견적·이관의 출발점이라는 걸 체감했습니다.
비전공(법학) 출신이라 오히려 '업무 흐름을 문서로 정리'하는 데
강점이 있다는 것도 알게 됐고요.

#기능분석 #FP #기능점수 #공공SI #레거시마이그레이션 #시스템분석
```

---

## 게시물 ④ — DB에 안 들어가는데 로그는 정상 (PK/트랜잭션)

> 2025년 차세대 행정심판 시스템 오픈·운영 당시 트러블슈팅.

```
"로그엔 분명 insert 완료가 찍혔는데, DB를 보면 데이터가 없습니다."

차세대 행정 시스템 오픈 직후 가장 당황스러웠던 장애였습니다.
처리 로그는 정상인데 실제 저장이 안 되니, 원인 추적이 까다로웠죠.

[진단]
하나의 등록 처리에서 두 개 테이블에 순차 insert가 일어나는데,
첫 테이블에서 생성한 PK(기본키)가 다음 단계로 전달되지 않고 있었습니다.
- 키 생성 구문은 실행됐지만 그 반환값을 서비스 로직이 들고 있지 않음
- PK가 빈 상태로 메인 insert가 수행 → 제약조건 충돌 → 조용히 롤백
- 그런데 로그는 앞단계까지만 찍혀 '정상'처럼 보였던 것

[조치]
- 키 생성 → 반환된 PK를 서비스 파라미터에 명시적으로 주입 →
  메인 insert에 그 PK를 실어 보내도록 트랜잭션 흐름을 재설계
- 이후 같은 패턴(다중 테이블 등록)을 표준 구조로 통일

[결과]
- 저장 정상화, 동일 유형 재발 0건

배운 것:
"DB에 반영이 안 된다"는 십중팔구 쿼리가 아니라
PK 처리와 트랜잭션 흐름의 문제다.
로그가 정상이라고 트랜잭션이 정상인 건 아니다 —
이걸 오픈 현장에서 몸으로 배웠습니다.

#백엔드 #트랜잭션 #데이터베이스 #공공SI #트러블슈팅 #MyBatis
```

---

## 게시물 ⑤ — 본안이 끝났는데 부속사건이 안 따라온다 (업무 CASCADE)

```
"본안 사건을 취하 처리했는데, 거기 딸린 신청사건은 그대로 살아있었습니다."

코드는 멀쩡한데 업무 규칙이 안 지켜지는, 전형적인 '업무로직 결함'이었습니다.

[상황]
행정 절차상 본안 사건이 취하종결되면,
거기 종속된 부속(신청) 사건도 같은 단계로 함께 종결돼야 합니다.
그런데 주 사건 상태만 바뀌고 종속 사건은 방치되고 있었습니다.

[진단]
주 사건 상태를 바꿀 때, 외래키로 엮인 종속 사건의 진행단계를
함께 갱신하는 로직 자체가 없었습니다.
이건 코드만 봐선 안 보이고, '업무 절차를 알아야' 보이는 결함이었죠.

[조치 — 핵심은 '전부 갱신'이 아니라 '선별 갱신']
1) 주 사건에 딸린 종속 사건이 있는지 먼저 조회
2) 그중 업무상 '같은 단계로 처리돼야 하는' 사건만 선별
3) 선별된 사건만 동반 갱신
→ "자식 선조회 → 조건 판별 → 동반 update" 패턴으로 표준화

[결과]
- 본안–부속사건 상태 정합성 확보, 누락 재발 없음

배운 것:
CASCADE성 처리는 기술이 아니라 업무 정의가 먼저다.
'어떤 자식을, 언제, 어떤 조건에서 갱신할 것인가'를
업무 담당과 합의하는 게 코드보다 중요하다.

#백엔드 #업무분석 #데이터정합성 #공공SI #트러블슈팅
```

---

## 게시물 ⑥ — 지웠는데 목록에 계속 보이는 데이터 (논리삭제)

```
"분명히 삭제했는데, 목록에 그대로 남아 있어요."

행정 데이터의 특성을 모르면 버그로 오해하기 쉬운 케이스입니다.

[배경]
행정 시스템은 데이터를 '진짜로 지우지' 않습니다.
이력·감사 추적 때문에 물리삭제 대신 '사용여부 플래그'를 끄는
논리삭제(soft delete)로 운영합니다.

[원인]
삭제 처리는 정상이었는데, 목록 조회 SQL의 조건절에
'사용중인 데이터만' 거르는 플래그 조건이 빠져 있었습니다.
그래서 꺼진(삭제된) 데이터까지 전부 조회된 것.

[조치]
- 조회 조건절에 사용여부 플래그 조건을 추가
- 같은 누락이 있을 다른 화면들도 함께 점검

[결과]
- 삭제 데이터 미노출 정상화

배운 것:
공공·행정 데이터에는 '물리삭제'라는 개념이 거의 없다.
논리삭제 시스템에서는 모든 조회 조건절에
사용여부 플래그가 들어가야 한다는 걸 원칙으로 익혔습니다.
도메인 규칙을 모르면 코드가 멀쩡해도 버그가 됩니다.

#백엔드 #SQL #도메인지식 #공공SI #데이터운영 #트러블슈팅
```

---

---

# PART2

## 첨부 A. 트러블슈팅 1-페이저 — 크롤러 회복탄력성

**도메인**: 정부 공공 데이터/여론 분석 시스템 (운영·유지보수)
**역할**: 외부 웹 수집 파이프라인 유지보수 및 템플릿 재설계
**기간**: 2026

| 문제 | 외부 매체의 SPA 전환으로 DOM 셀렉터 기반 수집 다수 실패. 수치 파싱 실패가 목록 렌더링 장애로 전이 |
| 원인 | ① 마크업 구조 변경 ② 게시판별 상이한 HTML ③ 수치 필드 예외처리 부재 |
| 해결 | JSON-LD 우선 파싱 + HTML 백업 이중화 / 영역 존재여부 분기 / 메타태그 우선 본문 추출 / 수치 정규식+기본값 표준화 / 구형 엔진 호환 보수적 코드 |
| 결과 | 개편 매체 수집 정상화, 빈값 목록 장애 재발 0건, 신규 매체 표준 템플릿화 |
| 사용 기술 | Java, Jsoup(HTML 파서), 정규표현식, JSON-LD, og:meta |
| 역량 | 외부 의존 시스템의 fallback 설계 / 장애 전이 차단 / 운영 표준화 |

---

## 첨부 B. 산출물 1-페이저 — 기능분해도(FP) 분석

**도메인**: 공공 정책·홍보/여론 분석 시스템 (차세대 전환 사전분석)
**역할**: 전 기능 기능점수(FP) 간이법 분해 및 문서화
**산출**: 2개 시스템, 수백 개 단위기능

| 단위 | 사용자 관점 단위 프로세스(대/중/소 메뉴) |
| 트랜잭션 | 조회/등록/수정/삭제(C·R·U·D) 분류 |
| 삭제 정책 | 물리삭제 vs 논리삭제 구분 표기 |
| 연계 | 외부 API 연계 여부 기능별 판별 |
| 데이터 | 참조 테이블 추적(JOIN 포함) — *실제 테이블명은 비공개* |

**증명 역량**: 대규모 시스템을 표준 기준으로 일관 분해 · 트랜잭션/삭제정책/연계 판단의 정형화 · 코드 레벨 구조 독해

---

## 첨부 C. 트러블슈팅 1-페이저 — 리포팅 툴 출력 커스터마이징

**도메인**: 공공 시스템 리포팅(상용 툴) 산출물
**문제**: 한글(HWP) 다운로드 시 글자/어절 단위 줄바꿈 제어 불가
**해결**: 툴 출력 속성·스크립트 단계적 변경 → 줄바꿈 동작 케이스별 검증 → 1·2차 수정안 비교 리포트화
**역량**: 상용 툴 커스터마이징 / 실험 설계(변경→출력→비교) / 발주처 합의용 산출물 작성

---

## 첨부 D. 트러블슈팅 1-페이저 — 차세대 시스템 오픈·운영 (행정 도메인)

**도메인**: 정부 차세대 행정 시스템 (구축·오픈·안정화, 2025)
**역할**: 3개 대형 시스템 통합 처리 / 일 SR 25~30건 / 권한 메뉴·화면 개발

| PK/트랜잭션 | 로그는 정상인데 DB 미반영 | 키 생성 PK를 서비스로 명시 전달, 다중테이블 등록 표준화 | 트랜잭션 설계 |
| 업무 CASCADE | 주 사건 종결 시 종속 사건 미연동 | 자식 선조회→조건 선별→동반 update 패턴화 | 업무 분석 + 데이터 정합성 |
| 논리삭제 | 삭제 데이터가 목록에 노출 | 조회 조건절에 사용여부 플래그 추가 | 행정 도메인 운영원칙 이해 |

**공통 강점**: 코드가 아니라 "업무 절차·데이터 운영원칙을 이해해야 풀리는" 결함을 다룸

---
